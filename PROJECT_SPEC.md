# AMATERAS — 現在の仕様

2026-09-07更新。日本語で日常の天気と防災情報を確認する一般利用者向け。初期実用版にLIVEのFX-0／FX-1を追加します。

## 今回の範囲

全国を初期表示する全面地図、地名検索、観測雨雲、現行気象警報、直近24時間の地震、現行台風、警報の立体面、実標高3D、暗色・明色、スマホ、起動可能なローカル版とWorkers公開準備。

参考動画の細い上部バー、小型カウンター、右約20%の一覧、小型タイムライン、下部ツール・ティッカーを踏襲。Palette A、白い幾何学文字と金色の弧を維持します。

## 情報の意味

- 警報は2026年の公式コード・名称。現行スナップショットの発表・継続・解除・切替を反映します。古い発表時刻だけでは失効させません。
- カウンターは区域コード×警報コード。地震・台風を加算しません。未知コードは未分類で、独自の危険度・高さを付けません。
- 区域面は同一区域の最も強い分類。低い分類の警報を選んでも面の重大度は下げず、輪郭と詳細で選択を表します。
- 一致する公式区域図形だけを描き、全国では警報を優先、注意報は拡大・選択時に取得。多数の情報は区域ごとにまとめ、ラベルと立体面の数を制限します。
- 立体面は各頂点に同じDEMの標高を与えます。高さは分類の視覚的強調です。公式レベル付き情報と、その他の警報・注意報の分類を区別します。
- 地震は発生時刻を基準に直近24時間。同一イベントの訂正・取消を反映し、座標不明は一覧だけに残します。選択パルスは最長4秒で、地震波の到達範囲ではありません。EEWは対象外。
- 台風は現行一覧を対象判定に使用。通常のVPTW61 XMLから実況中心、取得できた実況点、予報、予報円、非対称の風域を表示します。不明な区域を推測しません。実況経路は取得範囲を明示します。
- 雨雲は約3時間・5分間隔の観測のみ。未読込フレームへの切替を避け、欠損や予測を捏造しません。
- 4系統を独立更新。LIVE / PARTIAL / SOURCE ERROR / DELAYED / STALE等で状態を示し、失敗時は最終成功データを保持。データ発生・発表・取得・確認時刻を分けます。
- 雨雲の履歴表示は警報・地震・台風の時間を巻き戻しません。この違いを地図上に常時表示します。

## 3D・品質・操作

全端末の初期設定は3D＋Auto。手動の2D／3Dと品質を保存します。MapLibreの傾斜50度と標高1.25倍を使用し、2Dで真上へ戻ります。DEM失敗時は2Dと再試行操作を表示し、保存済みの手動設定は上書きしません。FX単体の失敗時は地図・雨雲・一覧を継続します。

Autoは700px以下でLow、その他でMediumから開始。負荷の継続時に段階的に下げ、15秒の待機期間を設けます。Lowは地形・区域面・輪郭・静止マーカーを維持し、立体メッシュとパルスを省きます。品質と各レイヤーは「表示」で変更できます。

非表示タブでは演出停止。OSの動きを減らす設定ではパルス・カメラ移動のアニメーション・ティッカーを静止させます。

## 小画面

1150px以下は折り畳み一覧、600px以下は下部シート。「気象警報／地震／台風」で共用します。スマホの一覧と雨雲タイムラインは同時に大きく開きません。選択時は展開中のシートを避けるカメラ余白を取ります。検索とタブのキーボード操作、フォーカス表示、モーダル、閉じた一覧のinertに対応します。

## 次段階

風・降水の立体化、雷・竜巻・津波・火山、河川・キキクル、GLB・PLATEAU・Three.js、SANDBOX、施設・番地検索、アカウント・通知。今回の追加コミット・プッシュ・公開デプロイも対象外です。

実装判断はTECH_ARCHITECTURE.md、検証の範囲と制約はIMPLEMENTATION_REPORT.mdを参照してください。

## iGPU向けグラフィック設定

初期プリセットはバランス。省電力／高画質へ切替でき、描画解像度・地形精度・陰影・地図詳細・立体品質・アニメーションを個別調整します。設定は端末保存し、2D／3Dの手動選択を保持。仕様と実測範囲は[グラフィック設定](GRAPHICS_SETTINGS.md)を参照してください。
