# AMATERAS — FX-0／FX-1 実装・検証レポート

検証日：2026-09-07（JST）。既存の実用版へLIVEの中核3Dを追加しました。このFX工程では公開デプロイ、追加コミット・プッシュ、有料契約を行っていません。

## 実装した内容

- 全端末で初期3D＋Auto。手動の2D／3Dと品質を端末保存。スマホのAutoはLowで開始します。
- MapLibre 5.24.0と国土地理院DEMを維持し、deck.gl 9.4.0を遅延読み込み。interleavedで描画コンテキストを共有します。
- 警報を区域ごとの最強分類へ集約。穴を保つ三角形分割、頂点ごとの実標高、屋根と側面のバッチ描画、選択優先のラベル制限を実装。低い分類の警報を選んでも区域の分類を下げず、未知コードには独自の高さを付けません。
- 地震は公式一覧の直近24時間を採用。更新・訂正・取消・座標欠損を扱い、震央・発生時刻・M・最大震度を表示。選択時だけ最大4秒のパルスを表示し、地震波の到達範囲ではないと明示します。
- 台風は現行一覧と通常発表のVPTW61を採用。実況中心、取得済み実況経路、予報点・予報円、方向別の強風域・暴風域を表示。単位・欠損・取消・現行一覧との時刻不一致を扱います。
- 地震／台風APIと独立した状態・キャッシュを追加。60秒確認、同時要求集約、長期フィード補完、原電文の再利用、部分失敗と最終成功保持に対応。XMLは検証後にキャッシュし、壊れた200応答を長期保持しません。
- 警報／地震／台風のタブ、共通スマホシート、各レイヤー切替、出典・時刻・凡例を統合。雨雲の履歴は観測レーダー専用で、警報・台風の最新取得と地震の24時間枠を併記します。
- 非表示時とreduced-motionでは演出を停止。DEM失敗は2D復帰と再試行、FXだけの失敗は地図・雨雲・一覧を維持します。

## 自動検証

| 実行 | 最終結果 |
| --- | --- |
| pnpm install --frozen-lockfile --offline --ignore-scripts --package-import-method=copy | ロックファイル変更なし、同梱パッチを含む依存関係は一致 |
| pnpm check | TypeScript成功 |
| pnpm test | **73件成功**、7ファイル。データ20、API7、再生7、タイル座標3、FX10、イベント21、DEM補完5 |
| pnpm test:e2e | **34件成功、失敗0、flaky 0**。6件はデスクトップだけで実施する2種類の負荷試験を他の3サイズで重複実行しないためのskip |
| pnpm build | 成功。メインJS 1,399.85kB（gzip 394.85kB）、遅延FX 824.59kB（gzip 232.94kB）、CSS 93.00kB |
| pnpm worker:check | Workerと14静的ファイルのdry-run成功。Worker 915.75KiB（gzip 157.79KiB）。公開なし |

固定入力の結果：[Vitest JSON](artifacts/fx/vitest-final.json)、[Playwright JSON](artifacts/fx/e2e-final.json)。E2Eでは背景・DEMも固定し、実通信から分離しています。Zodのコメント注釈に関する非致命的なビルド警告があります。

試験には警報の重複・解除・未知コード・区域不一致、1,000件以上の集約、地震の実形式と訂正・取消・EEW除外、台風の実XML・方向・単位・予報更新・活動終了、正常ゼロ件と取得障害の区別を含みます。雨雲は読み込み前の切替、先読み失敗、更新競合と100フレーム時のsource上限を検証しています。

UIは初期3D、設定保存、品質変更、reduced-motion、選択・テーマ復元、検索、スマホのシートとタイムラインの排他、連続選択、DEM失敗と再試行を確認。20回のテーマ／2D／3D切替でもcanvasのイベント数が一定で、地図canvasは1、レーダーsourceは最大3、固定スタイルのレイヤー数は一定でした。

## 実データ接続と本番ブラウザ

ViteとローカルWorkerのライブ接続は、それぞれ雨雲一覧・警報・雨雲PNG・区域・DEM・背景地図・地震・台風の**8接続**を確認します。[Viteログ](artifacts/live-smoke.json)、[Workerログ](artifacts/live-smoke-worker.json)を保存。TLS証明書の検証は有効です。

本番distをWorkerの8787番で開き、4系統のLIVE状態、地震・台風の一覧選択、暗色／明色、モバイル表示、初期3D、canvas 1個を確認しました。ブラウザ例外とCSP違反は0、MOCK DATAは表示されません。本番JavaScriptにも開発用の模擬雨雲プロトコル・模擬イベント本文を含みません。観測時の件数は地震5件・台風1件で、今後の更新で変動します。[本番ブラウザ記録](artifacts/fx/live-browser.json)。

台風の過去経路は取得できた公式実況点だけで、画面に取得範囲を表示。台風が現行一覧から終了した場合は対象から外します。新しい一覧と詳細が一致しない場合や不明な風域は部分取得として残し、架空の円・点を補いません。

## 比較画像と実地形

次の12画像は気象情報を開発用シナリオに固定し、背景地図とDEMは実際の配信を使用しています。すべてMOCK DATAを常時表示し、実際の災害を表す画像ではありません。横方向のはみ出し、3Dの維持、操作部分の配置を確認し、ブラウザ例外は0でした。[配置記録](artifacts/fx/layout-report.json)。

| サイズ | 平常 | 雨天 | 災害多発 |
| --- | --- | --- | --- |
| 1920×1080 | [画像](artifacts/fx/quiet-1920x1080.png) | [画像](artifacts/fx/rainy-1920x1080.png) | [画像](artifacts/fx/severe-1920x1080.png) |
| 1366×768 | [画像](artifacts/fx/quiet-1366x768.png) | [画像](artifacts/fx/rainy-1366x768.png) | [画像](artifacts/fx/severe-1366x768.png) |
| 768×1024 | [画像](artifacts/fx/quiet-768x1024.png) | [画像](artifacts/fx/rainy-768x1024.png) | [画像](artifacts/fx/severe-768x1024.png) |
| 390×844 | [画像](artifacts/fx/quiet-390x844.png) | [画像](artifacts/fx/rainy-390x844.png) | [画像](artifacts/fx/severe-390x844.png) |

地形追従と立体化は別に確認しています。固定試験は頂点ごとの正負の標高、穴、欠損面、三角形予算を検証。実DEMの画面確認では、いずれも50度の地形と立体面を維持しました。

| 区域 | 実地形と模擬警報の確認 | メッシュ面数（上面＋側面） |
| --- | --- | ---: |
| 八王子市・山間部 1320100 | [画像](artifacts/fx/mountain-warning.png) | 1,428 |
| 横浜市北部・沿岸 1410011 | [画像](artifacts/fx/coastal-warning.png) | 332 |
| 八丈町・離島 1340100 | [画像](artifacts/fx/island-warning.png) | 126 |

[スマホの台風シート](artifacts/fx/mobile-typhoon-sheet.png)では選択中心が画面Y=約217px、シート上端が約276pxで、中心が隠れません。地震から台風へ連続して選択する回帰テストも成功しました。標高は国土地理院データを表示上1.25倍にしています。

実気象の追加画像：[地震](artifacts/fx/live-earthquakes.png)、[台風](artifacts/fx/live-typhoons.png)、[明色](artifacts/fx/live-light.png)、[モバイル](artifacts/fx/live-mobile.png)。

## 資源計測と修正

初期の再生計測はRAF間隔の中央値16.7ms、p95 16.8msでした。今回の40回切替＋再生の計測では中央値17.7ms、p95 18.6ms。これはフレームを予約する間隔で、GPU描画時間や端末の保証FPSではありません。測定条件が完全に一致する比較試験でもありません。

修正前は、20回切替でGC後のJSヒープが約43.0MBから108.5MBへ増加しました。ヒープの参照経路を調べ、終了済みAnimationLoopがcanvasリスナーから保持され、FxManagerとメッシュを解放できないことを確認。luma.gl 9.4.0のイベント二重bind・解除漏れと頂点配列の解放を[同梱パッチ](patches/README.md)で修正し、MapLibreの地形も旧スタイルの置換前に明示的に破棄しました。

| 修正後の段階 | GC後JSヒープ（10進MB） | interval数 | canvas数 | FXレイヤー数 |
| --- | ---: | ---: | ---: | ---: |
| 起動後の暖機 | 43.69 | 10 | 1 | 1 |
| 10回 | 51.78 | 10 | 1 | 1 |
| 20回 | 51.75 | 10 | 1 | 1 |
| 30回 | 54.09 | 10 | 1 | 1 |
| 40回 | 53.80 | 10 | 1 | 1 |

後半で修正前のようなヒープの継続増加は再現しませんでした。待機時のFXアニメーションループは0。暗色58／明色116の地図レイヤー差は元スタイルの違いで、同じテーマへ戻ると同じ数です。[修正前](artifacts/fx/performance-before-fix.json)、[修正後40回](artifacts/fx/performance.json)。

WebGLの作成・破棄とJSラッパーのGCも観測しました。30→40回でBufferは427→382、VertexArrayは145→134、Textureは240→248。テクスチャは地図タイルの取得・再利用プールの暖機による変動を含み、全資源が起動直後の数へ戻ったという結果ではありません。VRAMのバイト数、長時間のGPU上限、実機スマホの性能は未計測です。初期に開発サーバーの更新を含む測定のため、厳密なGPUベンチマークとして使用しないでください。

## 実画面で見つけて修正した問題

- 八丈島周辺の高倍率DEMは海域タイルが404になる場合がありました。同じ地理院の低倍率タイルの該当区画を最近傍で切り出して補完。RGB標高を混色せず、通信障害・5xxは2D復帰のままです。
- 傾斜地図の平面用の中心補正だけでは、スマホの選択点がシートへ隠れました。対象中心・縮尺・シート余白をまとめて適用して修正しました。
- その後の連続選択では既存余白と追加余白が二重計算され、移動できないケースを再現。目的の表示領域を一度だけ使う縮尺計算に変え、地震→台風の操作で回帰確認しました。
- XMLのHTTP 200でも内容が不正なら原電文キャッシュへ採用せず、次の確認で再取得できるようにしました。

## 制約・次段階

- スマホはWindows版Chromiumで390×844などの画面サイズを検証しています。iOS／Android実機、全ブラウザ、低性能GPU、タッチジェスチャーの実機評価は未実施です。
- 地形の測定・目視は上記3区域と全国表示です。全1,863区域の悉皆検査や物理的な高度精度の保証ではありません。Lowでは立体メッシュを省略し、実地形・区域面・輪郭・静止マーカーを維持します。
- 計算予算や未読込標高によって立体面を省略する場合があります。公式の区域面・輪郭と一覧は維持。画像内のDEM欠損値は0mのため、局所的な粗さ・不連続が残ります。
- Autoは描画イベント間隔による段階的な品質低下です。GPUの実行時間を直接測る制御や自動昇格は行いません。
- JMA公開Webデータの仕様変更・停止に備えた検証と障害表示は実装済みですが、専用APIの保証契約はありません。Worker Cache APIは永続DBではなく、警報解除履歴・台風の全期間経路を永続保存しません。
- 実データ接続はローカルのVite／Workerまで。Cloudflare公開エッジ、ドメイン、公開負荷は未検証です。
- 風、降水の立体化、GLB、PLATEAU、SANDBOXなどFX-2以降は今回の対象外です。

起動・再現手順は[README](README.md)、取得先・利用条件・加工は[データ台帳](DATA_SOURCES.md)、設計と品質上限は[技術構成](TECH_ARCHITECTURE.md)を参照してください。ロックファイルと依存パッチは同梱し、比較画像・実通信ログはローカルのartifactsに保存しています。
