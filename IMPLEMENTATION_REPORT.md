# AMATERAS — 実装・検証レポート

検証日：2026-09-06。仕様資料・参考素材から実データで動くローカルアプリを実装しました。公開デプロイ、コミット、プッシュ、有料契約は行っていません。

## 実装した内容

- 全面地図、Palette A、白い文字と金色の弧、コンパクトな一覧とHUD。
- 気象庁の過去約3時間の観測雨雲、読込待ち付き再生、履歴選択、最新復帰。
- 2026年の現行r8警報、解除・重複処理、区域連動、公式地域索引1863件。
- 出典、取得時刻と発表・観測時刻、独立更新と障害時の最終成功保持。
- 国土地理院DEMによる3D地形、2D復帰、テーマ復元、スマホ下部シート。
- 同じAPI処理をViteとCloudflare Workerで使用。任意URLのプロキシは禁止。

## 自動検証

| 実行                              | 結果                                                |
| --------------------------------- | --------------------------------------------------- |
| pnpm test                         | 37件成功：データ20、API7、再生7、タイル座標3        |
| pnpm test:e2e                     | 8件成功：デスクトップ・スマホ各4件                  |
| pnpm check / pnpm build           | TypeScript成功、本番ビルド成功                      |
| pnpm worker:check                 | Workerと静的ファイルのdry-run成功。公開なし         |
| pnpm smoke:live（Vite）           | 雨雲、警報、雨雲PNG、区域、DEM、背景地図の6接続成功 |
| pnpm smoke:live（Workerローカル） | 同じ6接続成功                                       |

API試験は重複・解除・未知コード・区域不一致・取得障害と正常ゼロ件・独立キャッシュ等を固定入力で検証します。再生試験は順序、遅れて届いたフレーム、タイムアウト、先読み失敗、100フレーム時の最大3source/layerとリスナー解放を検証します。

Playwrightは検索・モーダル・平常状態、災害多発時のテーマ/パネル操作、取得失敗をゼロ件と誤認しない表示、DEM失敗時の2D復帰と連続再試行を検証します。テスト用Chromiumをプロジェクト内に導入しました。

本番JavaScriptにmock-radarプロトコルと模擬警報本文が含まれないことも確認しています。ビルドにはZodのコメント注釈に関する非致命的警告があります。出力はJS約1.37MB（gzip約384KB）、CSS約90KBです。

## 実データと画面確認

接続ログは [Vite](artifacts/live-smoke.json) と [Workerローカル](artifacts/live-smoke-worker.json) に分離しました。模擬シナリオの試験とは別の、証明書検証を有効にした実通信です。警報件数・雨雲の最新時刻は観測時点の値で変動します。

実ブラウザで検索、凡例、暗色/明色、雨雲の時刻進行と停止・最新復帰、富士宮市の実標高3D（55度）、2D復帰を確認しました。ブラウザエラーログも確認しています。

参考動画の5秒位置と既存コンタクトシート・パレットを目視確認。平常・雨天・災害多発の各シナリオを次の4サイズで撮影し、横方向のはみ出し、地図領域、下部ツールの位置を確認しました。

| サイズ    | 平常                                  | 雨天                                  | 災害多発                               |
| --------- | ------------------------------------- | ------------------------------------- | -------------------------------------- |
| 1920×1080 | [画像](artifacts/quiet-1920x1080.png) | [画像](artifacts/rainy-1920x1080.png) | [画像](artifacts/severe-1920x1080.png) |
| 1366×768  | [画像](artifacts/quiet-1366x768.png)  | [画像](artifacts/rainy-1366x768.png)  | [画像](artifacts/severe-1366x768.png)  |
| 768×1024  | [画像](artifacts/quiet-768x1024.png)  | [画像](artifacts/rainy-768x1024.png)  | [画像](artifacts/severe-768x1024.png)  |
| 390×844   | [画像](artifacts/quiet-390x844.png)   | [画像](artifacts/rainy-390x844.png)   | [画像](artifacts/severe-390x844.png)   |

追加画像：[実データ](artifacts/live-1366x768.png)、[明色](artifacts/live-light-1920x1080.png)、[実標高3D](artifacts/terrain-live-1920x1080.png)、[スマホ警報シート](artifacts/severe-mobile-sheet.png)。390×844のシートは幅390px、下端766pxで、下部ツールの上に収まることを確認しました。

## 接続検証で修正した点

- 雨雲は公式設定のzoomUse="even"に対応。奇数倍率では偶数倍率の親画像の該当四分区を切り出し、全国表示の透明タイル問題を解消。タイルの位置対応テストを追加。
- 旧警報URLは200でも5月の古いデータだったため、現行公式ページのr8 URLと33コードを採用。
- PowerShellでのTLS失敗に対し、実装経路のNode fetchで正常取得を確認。証明書検証を無効にしていません。
- Workerはredirect:error非対応。manualとHTTPステータス検証に変更し、外部への転送を拒否するテストを追加。
- 2D復帰時にDEM source/layerを解放し、取得失敗後の再試行ができるよう修正。全国移動のpitchも明示して操作競合を回避。

## 制約と未検証範囲

- スマホはブラウザ上の画面サイズ検証です。iOS/Android実機や全ブラウザの動作保証は未実施。
- 短時間の再生・テーマ切替とリソース数の単体試験は実施。数時間単位のGPU/ヒープメモリ推移、低性能端末での性能は未測定。
- 全国の注意報区域は拡大・選択時に取得。図形不一致は一覧に残し、推測図形を描きません。全1863区域の個別図形の悉皆検査はしていません。
- DEM欠損は0mへ変換。局所的な不連続や、地形・雨雲の拡大時の粗さがあります。
- JMA公開Webデータは本アプリ向けの安定API契約ではなく、仕様変更・停止があり得ます。Cache APIは永続DBではありません。
- アプリは現行発表を表示し、過去の解除履歴を永続保存しません。河川単位の氾濫情報等は今回の対象外。
- Workerはローカル実行とdry-runまで。実際のCloudflare配信、公開負荷、ドメイン設定は未実施。
